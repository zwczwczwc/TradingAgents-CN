# 前端适配详细设计文档

## 1. 分析师常量定义更新 (`frontend/src/constants/analysts.ts`)

为了实现**个股与指数分析的严格隔离**，我们需要为每个 Analyst 增加 `category` 属性。

```typescript
export interface Analyst {
  id: string
  name: string
  description: string
  icon?: string
  // 新增分类属性
  category: 'stock' | 'index' | 'common' 
}

export const ANALYSTS: Analyst[] = [
  // === 个股专用 Agent (Stock Only) ===
  {
    id: 'fundamentals',
    name: '基本面分析师',
    description: '分析公司财务状况、业务模式和竞争优势',
    icon: 'DataAnalysis',
    category: 'stock'
  },
  {
    id: 'news',
    name: '新闻分析师', // 这里的 News 指个股新闻
    description: '分析个股相关新闻、公告和市场事件',
    icon: 'Document',
    category: 'stock'
  },
  {
    id: 'social',
    name: '社媒分析师',
    description: '分析社交媒体情绪、投资者心理和舆论导向',
    icon: 'ChatDotRound',
    category: 'stock'
  },

  // === 指数专用 Agent (Index Only) ===
  {
    id: 'macro',
    name: '宏观分析师',
    description: '分析GDP、CPI、货币政策等宏观经济指标',
    icon: 'DataLine',
    category: 'index'
  },
  {
    id: 'policy',
    name: '政策分析师',
    description: '解读国家战略、产业政策及其对市场的影响',
    icon: 'Reading',
    category: 'index'
  },
  {
    id: 'sector',
    name: '行业分析师',
    description: '分析板块轮动规律与资金流向',
    icon: 'PieChart',
    category: 'index'
  },
  {
    id: 'technical_index', 
    name: '技术分析师(指数)',
    description: '基于K线、均线、成交量等指标分析大盘走势',
    icon: 'TrendCharts',
    category: 'index'
  },
  {
    id: 'intl_news',
    name: '国际新闻分析师',
    description: '监控全球地缘政治与外盘动态',
    icon: 'Global',
    category: 'index'
  },
  {
    id: 'bull_bear',
    name: '多空博弈',
    description: '模拟多头与空头辩论，深度挖掘市场分歧',
    icon: 'Connection',
    category: 'index'
  },
  {
    id: 'risk',
    name: '风险裁判',
    description: '综合评估市场风险，给出仓位控制建议',
    icon: 'Warning',
    category: 'index'
  },

  // === 通用 Agent (Common) ===
  {
    id: 'market',
    name: '市场分析师', // 既可以分析个股的市场表现，也可以分析大盘
    description: '分析市场整体趋势与情绪',
    icon: 'TrendCharts',
    category: 'common' // 如果需要更严格，可以将其拆分或归为 stock/index 之一
  }
]
```

## 2. 页面交互设计 (`SingleAnalysis.vue`)

### 2.1 模式切换
在页面顶部或表单开始处添加 `el-radio-group` 或 `el-tabs`：

- **选项 A**: 个股分析 (Stock Analysis) - 默认
- **选项 B**: 指数分析 (Index Analysis)

### 2.2 严格隔离逻辑 (Strict Isolation Logic)
当用户切换分析模式时，**Agent 选择区域必须强制刷新**，仅展示当前模式允许选择的 Agent。

1.  **模式 A: 个股分析 (Stock Mode)**
    -   **可选列表**: 仅展示 `category === 'stock'` 和 `category === 'common'` 的 Agent。
    -   **不可见/禁用**: 所有的 `category === 'index'` 的 Agent (如 Macro, Policy, Bull/Bear) **完全不可见**或置灰禁用。
    -   **默认选中**: `Fundamentals` + `News` + `Market`。

2.  **模式 B: 指数分析 (Index Mode)**
    -   **可选列表**: 仅展示 `category === 'index'` 和 `category === 'common'` 的 Agent。
    -   **不可见/禁用**: 所有的 `category === 'stock'` 的 Agent (如 Fundamentals, Social) **完全不可见**或置灰禁用。
    -   **默认选中**: `Macro` + `Technical(Index)` + `Policy` + `Bull/Bear`。

3.  **通用参数保留**:
    -   无论哪种模式，以下参数保持通用且可选：
        -   分析日期 (Analysis Date)
        -   LLM 模型 (LLM Model)
        -   语言 (Language)
        -   是否包含图表 (Include Charts)

### 2.3 深度参数与 Agent 执行逻辑 (Depth & Execution Logic)

针对指数分析，**分析深度 (Depth)** 不仅仅是 Prompt 的详略，它直接控制了**哪些 Agent 会被执行**。前端的深度选择本质上是“Agent 套餐”的快捷选择。

#### 深度与 Agent 映射关系

| 深度等级 (Depth) | 执行的 Agent 组合 | 适用场景 | 预计耗时 |
| :--- | :--- | :--- | :--- |
| **快速 (Quick)** | `Macro` + `Technical` | 盘中快速看一眼大势 | ~30s |
| **标准 (Standard)** | `Macro` + `Technical` + `Policy` + `Sector` | 盘后复盘，关注国内政策与板块 | ~2m |
| **深度 (Deep)** | Standard + `Intl_News` + `Bull_Bear` + `Risk` | 周末/大级别变盘点，全方位深度研判 | ~5m |

#### 前端交互优化
1.  **深度选择联动**: 当用户选择不同的深度时，下方的 Agent 列表应自动勾选对应的 Agent。
2.  **手动覆盖 (Override)**: 允许用户在选定深度后，手动勾选/取消某些 Agent（例如选了标准模式，但手动加上国际新闻）。此时深度选项显示为“自定义 (Custom)”。

## 3. 差异分析与改造补漏 (Gap Analysis)

### 3.1 出入参全景对比

| 维度 | 个股分析 (Stock Analysis) | 指数分析 (Index Analysis) | 改造动作 |
| :--- | :--- | :--- | :--- |
| **输入: 代码格式** | 6位数字 (e.g. `600519`) | 6位数字+后缀 (e.g. `000001.SH`) | 新增 `validateIndexStock` 验证器 |
| **输入: 市场类型** | A股/港股/美股 | A股指数/美股指数 | 下拉框需增加“指数”选项或根据后缀自动推断 |
| **输入: 核心参数** | `analysis_type='stock'` | `analysis_type='index'` | API 请求需显式传递 type 字段 |
| **输入: Agent组合** | 基本面 + 新闻 + 社媒 | 宏观 + 政策 + 行业 + 多空 + 风控 | 前端需严格隔离选择列表 |
| **输出: 核心字段** | `fundamental`, `sentiment` | `macro`, `policy`, `sector`, `debate`, `risk` | 扩展 `AnalysisResult` 类型定义 |
| **输出: 估值指标** | PE, PB, ROE (个股财务) | PE/PB 分位点 (全市场估值) | 结果页需增加估值分位图表支持 |
| **输出: 决策逻辑** | 线性评分 (Buy/Sell) | 对抗辩论 (Debate) + 风险裁判 | 新增“辩论视图”和“裁判视图” |

### 3.2 缺失的适配点 (Missing Adaptations)

#### A. 验证器增强 (`frontend/src/utils/stockValidator.ts`)
当前的 `validateAStock` 过于严格，只允许纯数字。需要新增 `validateIndexStock`：
```typescript
export function validateIndexStock(code: string): StockValidationResult {
  // 允许 000001.SH, 399001.SZ 等格式
  if (/^\d{6}\.(SH|SZ)$/.test(code)) {
    return { valid: true, market: 'A股指数', normalizedCode: code }
  }
  // 允许纯数字输入，但需提示用户补全后缀
  if (/^\d{6}$/.test(code)) {
     // ... 自动补全逻辑或提示 ...
  }
  return { valid: false, message: '指数代码格式应为 6位数字.SH 或 .SZ' }
}
```

#### B. 类型定义扩展 (`frontend/src/types/analysis.ts`)
`AnalysisResult` 需要兼容两种结构的返回数据：
```typescript
export interface AnalysisResult {
  // ... 公共字段 ...
  
  // 个股独有
  fundamental_analysis?: string;
  sentiment_analysis?: string;
  
  // 指数独有
  macro_analysis?: string;
  policy_analysis?: string;
  sector_analysis?: string;
  intl_news_analysis?: string; // 新增国际新闻
  debate_summary?: {
    bull_point: string;
    bear_point: string;
    conclusion: string;
  };
  risk_assessment?: {
    level: 'High' | 'Medium' | 'Low';
    suggestion: string;
  };
}
```

#### C. 结果组件分流 (`TaskResultDialog.vue`)
在渲染结果时，需要根据 `result.analysis_type` (或通过字段特征判断) 进行分流渲染：
- **If Stock**: 渲染 `Fundamental`, `Technical`, `Sentiment` 模块。
- **If Index**: 渲染 `Macro` (表格+文本), `Policy` (列表), `Debate` (左右对话框), `Risk` (仪表盘), `IntlNews` (时间轴)。

## 4. 结果展示设计 (`ReportDetail.vue` / `TaskResultDialog.vue`)

后端返回的 JSON 结构预计会包含新的字段。前端需做自适应展示。

### 4.1 报告映射配置 (Code Level Configuration)

为了让 `SingleAnalysis.vue` 和 `ReportDetail.vue` 能够识别并渲染指数分析的报告内容，需要在前端代码中补充以下映射配置。

#### A. `SingleAnalysis.vue` 中的 `reportMappings`
当前代码仅包含个股映射，需扩展指数相关字段：

```typescript
// frontend/src/views/Analysis/SingleAnalysis.vue

const reportMappings = [
  // === 原有个股映射 ===
  { key: 'market_report', title: '📈 市场技术分析', category: '分析师团队' },
  { key: 'sentiment_report', title: '💭 市场情绪分析', category: '分析师团队' },
  { key: 'news_report', title: '📰 新闻事件分析', category: '分析师团队' },
  { key: 'fundamentals_report', title: '💰 基本面分析', category: '分析师团队' },
  
  // === 新增指数映射 (Index Mappings) ===
  { key: 'macro_report', title: '🌍 宏观经济分析', category: '指数分析团队' },
  { key: 'policy_report', title: '📜 政策解读分析', category: '指数分析团队' },
  { key: 'sector_report', title: '🏭 行业板块分析', category: '指数分析团队' },
  { key: 'intl_news_report', title: '🌐 国际新闻分析', category: '指数分析团队' },
  { key: 'bull_bear_report', title: '🐂🐻 多空博弈辩论', category: '指数分析团队' },
  { key: 'risk_report', title: '🛡️ 风险控制评估', category: '指数分析团队' }
]
```

#### B. `ReportDetail.vue` 中的 `nameMap`
报告详情页的中文名称映射也需同步更新：

```typescript
// frontend/src/views/Reports/ReportDetail.vue

const nameMap: Record<string, string> = {
  // === 原有个股映射 ===
  market_report: '📈 市场技术分析',
  sentiment_report: '💭 市场情绪分析',
  news_report: '📰 新闻事件分析',
  fundamentals_report: '💰 基本面分析',
  
  // === 新增指数映射 ===
  macro_report: '🌍 宏观经济分析',
  policy_report: '📜 政策解读分析',
  sector_report: '🏭 行业板块分析',
  intl_news_report: '🌐 国际新闻分析',
  bull_bear_report: '🐂🐻 多空博弈辩论',
  risk_report: '🛡️ 风险控制评估'
}
```

### 4.2 报告结构概览
```json
{
  "analysis_type": "index_analysis",
  "summary": "...",
  "details": {
    "macro": { ... },
    "policy": { ... },
    "technical": { ... },
    "intl_news": { "news_list": [...], "impact": "..." },
    "debate": {
       "bull_point": "...",
       "bear_point": "...",
       "winner": "..."
    },
    "risk": {
       "level": "High",
       "suggestion": "Keep low position"
    }
  }
}
```

### 4.2 组件模块化
建议拆分为独立的 Vue 组件（Tab 页签形式展示）：

-   `<MacroView />`: 展示宏观经济指标表格 + 文本解读。
-   `<PolicyView />`: 展示政策列表（利好/利空/中性）。
-   `<SectorView />`: 展示板块资金流向热力图。
-   `<IntlNewsView />`: 国际新闻时间轴 + 影响评估 (新增)。
-   `<DebateView />`: 左右分栏展示多空观点，中间展示裁判结论。
-   `<RiskView />`: 仪表盘展示风险等级，高亮风控建议。

## 5. API 交互细节

### 请求参数构造
```typescript
const params = {
  stock_symbol: form.stockCode, // 指数代码
  parameters: {
    analysis_type: isIndexMode ? 'index' : 'stock', // 显式区分类型
    selected_analysts: selectedAnalysts, // 完整的分析师 ID 列表
    research_depth: form.depth,
    // ...
  }
}
```

后端需确保 `simple_analysis_service.py` 能正确识别 `analysis_type` 并路由到 `index_analysis_workflow`。
